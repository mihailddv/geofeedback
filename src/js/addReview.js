export function addReview(e) {
    let button = document.querySelector('.review-form__button');

    button.addEventListener('click', function() {
        e.preventDefault;

        console.log('test');
    })
}